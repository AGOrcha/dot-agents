# Distilled takeaways — operating-contract / model-routing batch (2026-07-13)

Three sources, full bodies in `research/articles/`. Full rubric evaluation:
`research/articles-evaluation-kg-and-adjacent.md` Part K. Plan-local consumer map:
`.agents/workflow/plans/transcript-analysis-and-pipeline-craft/evidence/prior/external-references-2026-07-12.md`
(2026-07-13 addendum). Genre caveat: all three are growth-account X articles with linkless
claims — they corroborate design shape, not effect sizes.

## 1. sairahul1: Fable brain backup (instruction distillation + transfer test)
- **Claim:** a stronger model's procedures can be extracted as second-person trigger→action
  standing orders ("When you see X, do Y"; advice must be rewritten until it's an action),
  loaded into a cheaper model, and the transfer PROVEN with a planted-defect trap test;
  failure routes to bounded single-section regeneration.
- **Extract:** instruction transfer is testable — when prompt/skill text carries capability
  across a model swap, verify the carry with a calibration task before trusting frontier
  output. → resolved into `disposable-tasks.md` Tier B (fold-back `lens-transfer-trap-test`).
  The monthly "summarize the instructions you're running on" health-check = the bundle
  readback probe. Trigger→action executability is the authoring bar for lens/skill prompts.

## 2. 0xmiraqle: GPT 5.6 god-mode contract
- **Claim:** goal + fence (5 house rules) + measurable bar, never the route; builder never
  grades its own work (5.6 system card: gamed graders at the highest rate measured on a
  public model [UNVERIFIED]); the loop never contains the model's right to declare itself
  finished; route sizes like a payroll; traces are "a private training set nobody else has";
  ~1-in-400 complex agent tasks go off-script → one budgeted human-QA hour daily.
- **Extract:** FOURTH independent invention of fresh-context builder≠grader (PostHog, honey,
  RULE-7, this) — and the first justified by measured grader-gaming, not taste. Chase the
  system card; if confirmed, cite in falsification-review-rubric prior art. Everything else
  corroborates existing design (verdict-gate termination, rules-as-fence, evidence-per-done,
  transcript-pipeline thesis).

## 3. prajwaltomar: two scoreboards (GPT 5.6 launch read)
- **Claim:** public benchmark (Sol 80, +2.8 over Fable) and Every's private senior-engineer
  benchmark (Fable 91, Sol 56) are BOTH true — well-defined vs ambiguous work are different
  sports ("Sol executes the instruction. Fable questions the instruction"). Converged org
  chart: judgment priced per-use at top, execution routed cheap; Anthropic's own numbers:
  orchestrator split keeps 96% performance at 46% cost [UNVERIFIED]. Effort dial > adjacent
  model choice. METR flagged Sol with their highest-measured reward-hacking rate.
- **Extract:** the popular argument for the plan's core epistemics — "benchmarks are
  marketing; run both on YOUR work" = preregistered contrasts on our own cells. 96%/46% is a
  chaseable effect-size prior for C1/C2. Reasoning-effort tier is a candidate future blocking
  axis (logged, not adopted mid-freeze). "Verify output especially on tests it wrote itself"
  = our frozen-verifier ground-truth rule.

## Cross-cutting (see Part K.4)
1. First full external convergence on the plan's three pillars (own-cell contrasts, RULE-7,
   model tiering) — consensus shape, and we're among the few instrumenting it.
2. Grader integrity now has two [UNVERIFIED] empirical hooks (5.6 grader-gaming, METR flag);
   verifying them is the batch's highest-value follow-up.
3. Instruction transfer is testable (trap test) — C0's logic applied to instructions.
4. New candidate axis: reasoning-effort tier (two independent claims it beats model choice).
