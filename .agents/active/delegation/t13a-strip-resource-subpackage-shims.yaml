schema_version: 1
id: del-t13a-strip-resource-subpackage-shims-1779826591
parent_plan_id: root-command-decomposition
parent_task_id: t13a-strip-resource-subpackage-shims
title: Delete mcp/settings/rules root shims + dead callers in cross-cutting tests
summary: Delegated from plan Root commands package decomposition
write_scope:
    - commands/mcp.go
    - commands/settings.go
    - commands/rules.go
    - commands/resource_parity_test.go
    - commands/coverage_test.go
    - commands/mcp_test.go
success_criteria: |-
    Split out of t13-strip-root-shims per worker fold-back
    (.agents/active/fold-back/t13-write-scope-missing-test-files.yaml).
    Smaller, mechanically-contained half: delete the three
    resource-subpackage shim files (mcp.go, settings.go, rules.go)
    which are pure re-exports now that t10a/b/c moved the prod
    code into commands/mcp/, commands/settings/, commands/rules/.

    Co-commit MUST include the dead callers per t12 fold-back:
      * commands/resource_parity_test.go: delete entirely (replaced
        by per-subpkg parity tests landed in t12).
      * commands/coverage_test.go: strip mcp/rules/settings RunE
        blocks ONLY (keep install/import/remove/review blocks for
        t13b).
      * commands/mcp_test.go: rewire to mcp.Deps + mcp.NewListCmd /
        NewShowCmd / NewRemoveCmd (or delete if duplicated by
        commands/mcp/cmd_test.go).

    Switch commands/root.go's mcp/settings/rules AddCommand calls
    from commands.NewMCPCmd / etc. to mcp.NewCmd / settings.NewCmd
    / rules.NewCmd. NOT in write_scope because root.go also wires
    install/doctor/init/status which t13b owns — that switch is
    deferred to t13b's pass to keep this PR small.

    Hard gate: go test ./... -race -count=1 green; per-file
    coverage gate stays clean (do NOT allowlist per
    [[no-lazy-allowlist-tech-debt]]; the deletions remove files
    from the gate's accounting, not lower their %).

    Anti-scope: do NOT touch lifecycle-cluster shim wiring (that's
    t13b). Do NOT touch root.go (t13b co-commit). If a deletion
    cascades into other test files, FOLD BACK rather than expand
    scope unilaterally.
verification_expectations: ""
may_mutate_workflow_state: false
owner: ""
status: active
created_at: "2026-05-26T20:16:31Z"
updated_at: "2026-05-26T20:16:31Z"
