schema_version: 1
task_id: adhoc-2026-05-27T12-36-04Z
parent_plan_id: adhoc
phase_1_decision: accept
phase_2_decision: accept
overall_decision: accept
failed_gates: []
reviewer_notes: |-
  ## KG Context (Step 0)

  ### kg changes --brief
  WARN: KG bridge unavailable for Step 0; per-file review will run without blast-radius context.
  Command reported that the code graph is not built and code-review-graph was not found in the isolated review PATH.

  ### kg impact <reviewed files>
  WARN: KG bridge unavailable for Step 0; per-file review will run without blast-radius context.
  Command reported that the code graph is not built and code-review-graph was not found in the isolated review PATH.

  ## Per-file review

  - Embedded loop-worker starter profile, agent, and skill instructions:
    accepted. They now describe only the legacy/full-slice compatibility path,
    prohibit typed ISP stage use, and keep delegated closeout parent-owned.
  - Embedded ISP and iteration-close instructions: accepted. Typed stages no
    longer inherit the legacy active-loop overlay or loop-worker prompt, and
    accepted delegation closeout no longer instructs the parent to run a
    duplicate `workflow advance`.
  - `internal/scaffold/home/copy_test.go`: accepted. The new assertions guard
    the materialized starter assets against reintroducing the legacy/staged
    instruction collision and duplicate closeout ownership.
  - Proposal and configuration distribution spec additions: accepted. They
    identify the important remaining boundary: corrected embedded seeds fix
    new installations, while existing customized `~/.agents` assets require
    an explicit, provenance-aware, non-destructive migration flow.

  ## Review checks

  - Code quality and maintainability: pass; the guidance uses one consistent
    legacy-versus-typed boundary and adds a focused regression helper.
  - Security and performance: pass; no new external input, privilege,
    dependency, or runtime hot-path behavior is introduced.
  - Gotchas: pass; no debug markers or secret-like material found in changed
    files, and the broad starter scan found no remaining stale injection or
    delegated `advance` instruction.
  - Verification: `git diff --check` passed; `go vet ./...` passed with
    isolated HOME/AGENTS_HOME and existing module cache; `go test ./...`
    passed with the same isolation and a PATH omitting only the broken
    optional user-local `code-review-graph` launcher.

  Residual planning work: native named-stage materialization and installed
  starter-seed migration are intentionally specified for canonical planning;
  this commit does not silently rewrite customized installed config.

  ## §1.6 telemetry envelope

  ```yaml
  resource_type: skill
  resource_id: self-review
  invoked_at: "2026-05-27T12:36:04Z"
  invoked_by: orchestrator
  task_id: adhoc-2026-05-27T12-36-04Z

  outcome:
    declared: success
    agent_self_assessment: "Embedded starter guidance is aligned with the canonical legacy-versus-staged boundary and parent-owned closeout, while installed-config migration remains explicitly planned."

  post_invocation:
    agent_actions_after_skill_returned: []
    user_corrections: []
    retries_in_loop: 0

  improvement_signals:
    missing_in_skill: []
    redundant_in_skill: []
    tooling_gap: { present: true, note: "KG Step 0 was unavailable in isolated review PATH; no graph build was performed during self-review." }
    script_gap: { present: false, note: "" }
    instruction_gap: { present: false, note: "" }
  ```
recorded_at: "2026-05-27T12:36:04Z"
recorded_by: self-review (skill)
