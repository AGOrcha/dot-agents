# Lessons index

One line per lesson. See each `LESSON.md` for the full pattern, root
cause, rule, and how-to-apply.

- [`workflow-task-update-replaces-notes`](./workflow-task-update-replaces-notes/LESSON.md): `da workflow task update --notes` REPLACES the notes field (no append) — it silently clobbers existing rich notes. Read current notes first and reconstruct original + addition, or edit the `notes:` block scalar directly; `git diff` before committing.

- [`foldback-update-clobbers-proposal-body`](./foldback-update-clobbers-proposal-body/LESSON.md): `da workflow fold-back update` on a PROPOSAL-routed fold-back rewrites the destination `~/.agents/proposals/obs-<slug>.md` body with the new observation (destroys the original, often in an untracked tree). Sibling of `workflow-task-update-replaces-notes` — same replace-by-default clobber family. To archive a proposal-routed fold-back, `git mv` the staging artifact only; never `fold-back update`. Recover a clobbered proposal from the staging artifact's add-commit in git.

- [`tests-must-drive-the-production-path`](./tests-must-drive-the-production-path/LESSON.md): A green test that doesn't exercise the production path (tautological comparison, lifecycle manually driven in the test, or a callable-but-uncalled gate) certifies nothing — drive the public entry point, compare independent readbacks, integration-test gates at the call site, and mutation-verify the fix. Caught 3× in one wave by cross-harness review, never by same-model self-review.
- [`hermetic-env-for-cli-probe-tests`](./hermetic-env-for-cli-probe-tests/LESSON.md): Unit tests that drive CLI-detection code must pin a hermetic base PATH in package `TestMain` (toolchain + system dirs, minus the agent-CLI dirs) — inheriting the real PATH leaks real `--version` subprocesses, a machine-dependent gate timeout invisible on CI.
- [`worktree-sibling-path-buildvcs`](./worktree-sibling-path-buildvcs/LESSON.md): Worktrees at sibling paths outside the repo root break `go build` VCS stamping in the pre-push gate — keep worktrees under the repo (e.g. `.agents/worktrees/`) or pass `-buildvcs=false` deliberately.
- [`tests-for-each-slice`](./tests-for-each-slice/LESSON.md): Each implementation slice should add automated coverage for the behavior it introduces before the slice is considered complete.
- [`additive-state-fields`](./additive-state-fields/LESSON.md): When extending a central state struct, add a new field rather than reusing an existing one — keeps JSON contract stable and callers unbroken.
- [`test-string-count-ids`](./test-string-count-ids/LESSON.md): When checking for duplicate entries in a markdown index, count `"- [id]"` (the entry prefix) not the raw ID string — the ID can appear multiple times in a single valid link entry.
- [`kg-test-fixture-helper`](./kg-test-fixture-helper/LESSON.md): KG tests needing a populated graph should use `setupKGWithNotes(t)` — don't re-seed notes inline per test.
- [`test-stub-upgrade-pattern`](./test-stub-upgrade-pattern/LESSON.md): When upgrading a stub to a real implementation, grep for tests asserting stub side-effects and update them before running the full suite.
- [`refresh-import-before-relink`](./refresh-import-before-relink/LESSON.md): Refresh flows that import repo files and regenerate managed outputs must import project content before relinking, and must not replay canonical backup snapshots as live state.
- [`verify-managed-file-target`](./verify-managed-file-target/LESSON.md): When a task touches a managed file path, use the intended managed generation flow first; if a repo-local file is still required, verify the final path is not a generated managed link before closing the task.
- [`use-skill-architect-for-skill-generation`](./use-skill-architect-for-skill-generation/LESSON.md): When generating or restructuring skills, use the skill-architect workflow so skills are valid before they are treated as usable or promoted to managed resources.
- [`archive-completed-active-plans`](./archive-completed-active-plans/LESSON.md): When a plan is complete, move it out of `.agents/active/` into the matching history folder so the active set reflects current work instead of finished tasks.
- [`lint-check-count-assertion`](./lint-check-count-assertion/LESSON.md): When adding a new lint check, grep for `ChecksRun` count assertions in tests and update them to `N+1` before running the suite.
- [`sidecar-manifest-pattern`](./sidecar-manifest-pattern/LESSON.md): Store integrity hashes in a sidecar manifest file keyed by ID, not inline in the file being hashed — avoids self-referential hash problem.
- [`rfc-resolves-plan`](./rfc-resolves-plan/LESSON.md): Plans gated on "requires RFC" often already contain the answers — write a brief RFC and implement in the same session instead of spending a full cycle on research.
- [`analysis-gaps-need-tasks`](./analysis-gaps-need-tasks/LESSON.md): When a session produces analysis results (A/B comparisons, gap findings, identified bugs), those findings must become canonical tasks before the session closes — prose notes are not tasks and don't get scheduled.
- [`select-star-scan-order`](./select-star-scan-order/LESSON.md): When porting DB schemas to Go, SELECT * scans must match exact column order — enumerate columns explicitly or count and comment them inline.
- [`use-existing-subdir-helpers`](./use-existing-subdir-helpers/LESSON.md): In kg.go, use `noteSubdir(t)` not `noteType+"s"` — "entity" maps to "entities" not "entitys".
- [`symbol-only-references-in-artifacts`](./symbol-only-references-in-artifacts/LESSON.md): Workflow artifacts reference code by symbol/function/relational anchor — never line numbers. Line numbers drift the instant any sibling edit lands.
- [`test-file-naming`](test-file-naming/LESSON.md) — test files mirror source/feature; iteration-numbered grab-bag test files (`coverage_pushN`, `ci_driftN`, …) are forbidden.
- [`subagent-out-of-workspace-access`](subagent-out-of-workspace-access/LESSON.md) — spawned subagents are sandboxed to the workspace root; pre-authorize external dirs or read inline.
- [`stale-local-master-ref`](stale-local-master-ref/LESSON.md) — audits must verify against origin/<branch>, not the stale local ref (caused a 3-way cascade).
- [`concurrent-workers-one-worktree`](concurrent-workers-one-worktree/LESSON.md) — one active writer per worktree; workers must not self-arm CI waiters (orchestrator polls + relays).
- [`prefer-interface-di-over-funcvar-seams`](prefer-interface-di-over-funcvar-seams/LESSON.md) — interface-DI is the preferred test seam; func-var `var osX = os.X` is legacy; prevalence ≠ sanctioned (audits must state descriptive vs prescriptive).
- [`match-ci-test-flags-locally`](match-ci-test-flags-locally/LESSON.md) — verify concurrency changes with CI's exact flags (`-race -count=1`) before push; lazy WaitGroup.Add needs mutex+closed ordering vs Wait.
- [`credstore-loader-test-hangs-on-macos-keychain`](credstore-loader-test-hangs-on-macos-keychain/LESSON.md) — a unit test that drives a real `credstore.NewLoader()` resolution passes on linux/windows but TIMES OUT on the macOS CI shard (the keyring step blocks on the macOS Security Service on a headless runner). Assert fallback wiring by TYPE (`_, ok := r.(*credstore.Loader)`), or exercise the chain hermetically with `WithKeyring(nil)` + `DA_CREDENTIALS_FILE` — never resolve through the real keychain in a test.
- [`cf-worker-auth-gate-fail-closed`](cf-worker-auth-gate-fail-closed/LESSON.md) — fail-closed checklist for auth-gating a Cloudflare Worker + static-assets (from an adversarial review of the docs CF Access gate): static assets BYPASS the worker unless `run_worker_first: true` (the per-route array misses bare `/internal` + encoded variants); classify paths fail-closed after decode+normalize (`/internal%2f..`, `/INTERNAL`, `//internal`, dot-segments, NUL all gate); allowlist public files, never carve out a whole dir (a blanket `/schemas/*` leaked internal DTO schemas); attach bearer-equivalent headers HTTPS-ONLY (refuse before resolving the secret); hand-rolled JWT must pin RS256/reject none+HS, require non-empty kid, kty=RSA, enforce exp/nbf/iss/aud, fail closed on every error. Verify with a Miniflare real-runtime smoke (`routerConfig.invoke_user_worker_ahead_of_assets`), not a stubbed-binding unit test or `serve dist` (both bypass the worker).
- [`validate-bundle-against-head`](validate-bundle-against-head/LESSON.md) — TASKS.yaml status/notes/write_scope decay between snapshots; HEAD-validate write_scope paths AND task premise before `workflow fanout`, especially after history rewrites.
- [`worker-owns-pr-readiness-loop`](worker-owns-pr-readiness-loop/LESSON.md) — loop-workers self-monitor + auto-fix CI/Sonar issues until PR is review-ready; parent context only sees terminal READY or FOLD-BACK.
- [`verify-task-status-vs-pr-history`](verify-task-status-vs-pr-history/LESSON.md) — pre-fanout: cross-check `gh pr list --state merged --search <task-id>` and dependent-task statuses; TASKS.yaml lies after parallel waves. `delegation closeout --decision accept` auto-advances; `workflow advance` is for direct (non-delegated) work.
- [`loop-worker-vs-general-purpose`](loop-worker-vs-general-purpose/LESSON.md) — loop-worker needs a delegation bundle; use general-purpose subagent for ad-hoc PR-shipping work without a canonical task.
- [`starter-vs-project-overlay`](starter-vs-project-overlay/LESSON.md) — generic starter ships how-to-USE `da` (the product); dot-agents dev overlay holds how-to-DEVELOP dot-agents (gh, /tmp/dev binary, Sonar gates).
- [`no-lazy-allowlist-tech-debt`](no-lazy-allowlist-tech-debt/LESSON.md) — coverage-exceptions.txt entries without a scheduled cg6 task are silent regression of the 95% gate; "not my work" needs scope+plan+schedule.
- [`parallel-worker-branch-drift`](parallel-worker-branch-drift/LESSON.md) — prek pre-commit hooks can land a worker's commit on a sibling worktree's branch via stash/restore; verify branch HEAD matches worker's claimed commit post-push.
- [`bundle-scope-via-code-graph`](bundle-scope-via-code-graph/LESSON.md) — pre-flight write_scope with `mcp__code-review-graph__file_summary` + grep for unexported symbols before `workflow fanout`.
- [`worktree-no-cd`](worktree-no-cd/LESSON.md) — multi-worktree sessions: always `git -C <path>` and never `cd` (Bash pwd persists across calls and cross-wires worktree state).
- [`init-bypasses-hooks-package`](init-bypasses-hooks-package/LESSON.md) — `commands/init.go` writes Claude/Cursor global settings/hooks directly; should route through the hooks package + restore settings+hooks merging.
- [`const-extraction-triggers-cpd-on-tables`](const-extraction-triggers-cpd-on-tables/LESSON.md) — fixing S1192 dup literals via const extraction can trigger Sonar CPD when the literal sits inside a tabular layout; data-driven refactor (loop over typed slice) is required.
- [`sonar-rating-gate-misses-new-issues`](sonar-rating-gate-misses-new-issues/LESSON.md) — SonarCloud's "Sonar way" gate is rating-based; doesn't fail on individual CRITICAL smells if rating stays A; must add custom gate with new_violations > 0 → FAIL; agent-side pr-ci-watch verifier independently checks count.
- [`verifier-owns-ci-watch-shift-left`](verifier-owns-ci-watch-shift-left/LESSON.md) — for dot-agents project overlay: register a `pr-ci` verifier_profile (alongside `unit`/`api`/`batch`) in .agentsrc.json + author the prompt file. Existing global `verifier` agent owns gh+Sonar polling per the profile; auto-fixes mechanical issues, escalates non-mechanical to impl. Impl exits at merge-back. Audit at .agents/proposals/pr-ci-verifier-integration-audit.md.
- [`agents-lack-autonomous-timers`](agents-lack-autonomous-timers/LESSON.md) — Claude Code agents have no autonomous timer; "every Ns" charter language silently fails. v1: piggyback on every incoming message. v2: workflow-orchestrator-daemon owns the scheduler.
- [`venv-symlink-into-worktrees-pre-adapter`](venv-symlink-into-worktrees-pre-adapter/LESSON.md) — symlink the repo `.venv` into every new worktree before push hooks fire; pre-push coverage-gate runs `go test ./...` which transitively needs CRG's Python venv. Expires when graph-backend-adapter-contract lands.
- [`consolidate-vestigial-siblings-on-rename`](consolidate-vestigial-siblings-on-rename/LESSON.md) — a "rename/generalize this primitive" request usually means "consolidate the duplication around it"; audit for duplicate-shaped fields + superseded-but-not-retired surfaces (and their lagging consumers) before planning, instead of scoping to the literal rename.
- [`merge-back-frontmatter-required`](merge-back-frontmatter-required/LESSON.md) — `da workflow delegation closeout` rejects merge-back artifacts missing any schema_version=1 frontmatter field; `files_changed:` MUST be `[]` or block-list. Silent rejection on first hit.
- [`dep-routing-on-partial-start-signals`](dep-routing-on-partial-start-signals/LESSON.md) — user "start X (since Y is …)" means "make X eligible per dep routing," not "force-spawn now." Verify Y ≥ awaiting_owner_review before fanout; else queue X as blocked-on:Y per layered-pr-fanout §3.4.
- [`stale-local-checkout-mass-drift`](stale-local-checkout-mass-drift/LESSON.md) — a local checkout many commits behind origin accumulates spurious working-tree deletions + mixed dirty state; `da`/`eligible` then read stale plans. Recovery: backup-branch the dirty tree, hard-sync local to origin/master, re-apply genuinely-local-only authored docs. Sibling of [[stale-local-master-ref]].
- [`ssh-keepalive-for-slow-pre-push-hook`](ssh-keepalive-for-slow-pre-push-hook/LESSON.md) — push fails `Connection ... closed by remote host` AFTER green hooks because the slow pre-push hook (sonar-scanner etc.) idles the SSH connection past GitHub's timeout. Fix: `GIT_SSH_COMMAND="ssh -o ServerAliveInterval=15 -o ServerAliveCountMax=30"` — never `--no-verify`. Always verify the ref landed with `git ls-remote`.
- [`sonarcloud-gate-mechanics`](sonarcloud-gate-mechanics/LESSON.md) — free-tier SonarCloud has no branch analysis (local scanner evaluates working-tree-as-main → keep main green); editing a file resurfaces its old issues as new-code; fixes can trade findings (`ADD --chown`→S6504, `wget --https-only`≠disable-redirects, `--ignore-scripts` may break builds); CE "Task finished abnormally" is a retryable infra flake; execabs deprecated (Go 1.19+) so go:S4036 PATH probes are reviewed-safe. Sibling of [[ssh-keepalive-for-slow-pre-push-hook]].
- [`sonar-mcp-security-filter-under-reports`](sonar-mcp-security-filter-under-reports/LESSON.md) — when `new_security_rating>A` fails the gate but the Sonar MCP `impactSoftwareQualities=SECURITY` query returns 0 issues, it is usually NOT a flake: the MCP impact filter omits legacy `type=VULNERABILITY` findings (e.g. `githubactions:S6505/S8543/S8233`). Query the API directly with `types=VULNERABILITY` (BUG/CODE_SMELL for the other ratings) BEFORE concluding flake, then fix (e.g. `npx`→`npm exec --no`, workflow→job permissions) — no suppressions.
- [`proactively-apply-built-capabilities`](proactively-apply-built-capabilities/LESSON.md) — after creating/improving a skill/agent/audit capability, proactively RUN it on the live project (dogfood immediately), don't build-and-move-on. "We just built X" is the trigger to "now run X and route its output." Evidence: the release-docs-refresh audit matrix, once finally run, found drift in 4/6 categories in one parallel sweep.
- [`leverage-cross-platform-fs-helpers`](leverage-cross-platform-fs-helpers/LESSON.md) — route OS-divergent fs behavior through `internal/fsops` (mutations) and `internal/testutil.MakeDir/FileUnreadable` (forced access errors); never branch on `runtime.GOOS` or use the NTFS-ignored `0o000` trick — single-OS-green hides Windows-red.
- [`worktree-isolation-defeats-status-tracking`](worktree-isolation-defeats-status-tracking/LESSON.md) — workers in isolated worktrees update task status in their own worktree, invisible to the main-repo scout → re-dispatch storms (5×duplicate PRs). Fan-out scouts must exclude open-PR tasks + keep a cross-wave dispatched-set; null-guard the scout; the real fix is a shared coordination backend.
- [`pin-release-toolchain-and-make-releases-retryable`](pin-release-toolchain-and-make-releases-retryable/LESSON.md) — pin every build/signing/timestamp tool to an exact version (never `--prerelease`/unpinned; an unpinned signing tool caused the Linux `kernel32`/`DLSequence` crash) + add a CI guard that fails on unpinned installs; make releases idempotent (delete the stale tag + `workflow_dispatch` re-drive on infra failure, don't poison on tag-exists). Sibling of [[gates-must-be-locally-reproducible]].
- [`hermetic-home-for-state-resolving-tests`](hermetic-home-for-state-resolving-tests/LESSON.md) — any test that resolves `$HOME`/graph-home/state-dir/platform-dirs/keychain MUST `t.Setenv` an isolated `t.TempDir()` env (HOME/USERPROFILE/XDG_*) so it resolves only paths it created; root cause of the flaky `TestRunWorkflowPlanDeriveScopeDegradesGracefully` that forced `--no-verify`. Generalizes [[credstore-loader-test-hangs-on-macos-keychain]]; PATH sibling [[hermetic-env-for-cli-probe-tests]].
- [`gates-must-be-locally-reproducible`](gates-must-be-locally-reproducible/LESSON.md) — if a CI gate (Sonar etc.) isn't locally reproducible, fix THAT (hermetic exclusions for `dist/`/`.scannerwork`, a runnable local invocation, install the hook) instead of CI-round-tripping or bypassing with `--no-verify`; the sonar dist/.scannerwork flake drove the bypasses. Sibling of [[sonar-container-fsmonitor-socket]], [[sonarcloud-gate-mechanics]].
- [`live-smoke-must-run-on-every-target-os`](live-smoke-must-run-on-every-target-os/LESSON.md) — unit tests passing on an OS (even ~98.5% line coverage) and a live smoke whose harness pre-creates the happy-path dir are NOT the same as the assembled binary running a core lock/fs/path command on that OS under real first-run preconditions; the Windows `agentslock` acquire bug (`mkdir .agentsrc.lock.lock: cannot find the file specified`) shipped through green windows-latest `go test` AND a green `verify.sh` that `mkdir -p`'d the lock's parent. Run the real-binary first-run smoke unguarded on every target OS, assert content+exit, mechanize the raw-`os.*`→`fsops` rule. Sibling of [[leverage-cross-platform-fs-helpers]], [[hermetic-home-for-state-resolving-tests]]; see `.agents/history/rca-windows-agentslock-escape.md`.
- [`single-source-of-truth-across-specs-and-plans`](single-source-of-truth-across-specs-and-plans/LESSON.md) — keep each concern in ONE canonical spec/plan; sibling specs/plans that touch the same area must POINT to it, never re-state it. Splitting/duplicating one concern across overlapping docs (e.g. `config-v2-migration` ↔ `config-v2-coherence`) drifts the copies; a task then carries a STALE spec detail referencing a superseded design, a worker implements off it and burns a cycle. When refreshing/activating a design-phase spec/plan, run a coherence reconciliation: verify each detail vs ACTUAL implementation (read the code), single-source duplicated concerns into pointers, fix stale spec-details embedded in TASKS, fold new ideas into the canonical doc. Design-phase sibling of [[validate-bundle-against-head]], [[stale-local-master-ref]], [[consolidate-vestigial-siblings-on-rename]].
- [`prototype-experiment-fidelity-gate`](prototype-experiment-fidelity-gate/LESSON.md) — a prototype whose tests pass can still prove the wrong thing; gate every experiment-backed decision on fidelity (faithful inputs + a real negative control + real execution + no hidden losses + an independent cross-harness audit). Hardened by the depth-degradation arc (v1–v4): four discrimination levels (instrument/experiment/regime/power), two-gate cross-brain (audit the DESIGN pre-run + the CONCLUSION post-run), null is first-class, fold narrow + caveated. Operationalized as the domain-general scientific-method spine of the `ideation-cycle` skill (`.agents/proposals/scientific-method-spine-domain-general.md`). Experiment-level lift of [[tests-must-drive-the-production-path]].
- [`cross-harness-read-raw-not-reconciler`](cross-harness-read-raw-not-reconciler/LESSON.md) — a same-family cross-harness *reviewer* laundered Codex's `fail` (BLOCKER+HIGH) into "clean pass" TWICE on t6-oci-consume; the reconciler's own-model priors demote/rationalize the other brain's findings, defeating the point. Require the raw alternate-harness output as a cited artifact, READ it yourself, treat every other-brain BLOCKER/HIGH as real until you personally disprove it, and never ship on the reviewer's own-trace verdict when it couldn't read the alternate back (lock/timeout = same-family review, re-dispatch). Nearly shipped 3 security bugs. Review-agent-trust sibling of [[tests-must-drive-the-production-path]].
- [`destructive-prune-fail-closed-on-incomplete-keepset`](destructive-prune-fail-closed-on-incomplete-keepset/LESSON.md) — a GC/prune that deletes the complement of a keep-set must fail closed when the keep-set is incomplete; "conservative under-collect" of the KEEP set = OVER-delete (data loss). CAS orphan GC skipped any unreadable project lock and `RemoveAll`'d a live artifact. Keep-set must be complete-or-error (distinguish `os.IsNotExist`=safe-empty from read-error=propagate); the deleter aborts on an incomplete set; test the present-but-unreadable case and assert the referenced entry survives. Caught by [[cross-harness-read-raw-not-reconciler]]; sibling of [[tests-must-drive-the-production-path]].
- [`closeout-before-clearing-iteration-sentinel`](closeout-before-clearing-iteration-sentinel/LESSON.md) — `delegation closeout` archives the merge-back but does NOT clear the `iteration-close-gate` hook-sentinel that references it → the Stop/PreToolUse guard hard-blocks EVERY tool until the file reappears (deadlocked a whole session ~20 cycles). A worktree worker running `/iteration-close` from the main checkout writes its sentinel to main; a round-2 resume after closeout writes a born-invalid sentinel (merge-back rejected — delegation already `completed`). Clear sentinels as part of closeout (`hook-sentinel clear --run-id`; they stack — re-read until none); if deadlocked the escape is a user `!` shell command, not a tool. See `.agents/proposals/delegation-reopen-and-self-documenting-sentinel-gate.md`. Sibling of [[worktree-isolation-defeats-status-tracking]].
- [`shipped-content-must-be-self-contained`](shipped-content-must-be-self-contained/LESSON.md) — scaffolded skills/agents under `internal/scaffold/home/starter/` ship to `da` end-users who do NOT have the dot-agents repo, so any reference to a *specific named* contributor artifact (`.agents/proposals/<id>`, `.agents/workflow/specs/<named>/design.md`, a bare lesson name like `prototype-experiment-fidelity-gate`, `evidence/<...>` sidecars + `sidecar vN` citations) dangles for them. Rule: shipped content is self-contained or references only co-shipped sibling files, `da` commands, or `<placeholder>` user-runtime paths — remove pure provenance, reshape useful pointers to a co-shipped file, keep the method/finding. Grep the scaffold for repo-internal refs before shipping. Follow-up: `delegation-lifecycle/instructions/workflow.md` cites `.agents/workflow/specs/agent-ops-hardening/design.md` (on master). Reference-integrity sibling of [[starter-vs-project-overlay]] and [[symbol-only-references-in-artifacts]].
- [`verify-plan-readiness-against-canonical-ref`](verify-plan-readiness-against-canonical-ref/LESSON.md) — "plans/tasks ready to implement?" must be verified against the CANONICAL ref (origin/master specs + latest merged coherence/redesign PRs), and task NOTES diffed against the spec they claim to implement (`git log -1 --format=%h` spec vs TASKS.yaml); a merged spec redesign (#162 §2A/EventBus) silently invalidated the r3 task notes, a full wave was built on them, and the cross-brain gate rejected it wholesale — reconcile notes (dated `RESCOPED` prefix) BEFORE fanout. Temporal sibling of [[single-source-of-truth-across-specs-and-plans]].
- [`error-audit-whole-command-sequence`](error-audit-whole-command-sequence/LESSON.md) — swallowed-error audits must cover EVERY step of a command pipeline (staging preludes, not just the headline `commit`/`push`) AND void helper functions that run failable commands (give them an `error` return + propagate before any destructive next step); se5 (#356) fixed `git commit`/`git push` but left `git add -A` `.Run()` and push.go's void `stageAndCommit` swallowing add+commit — caught post-merge by the user reading the diff. Distinguish real swallows (proceed-on-false-success) from benign display/status `.Output()` reads.
- [`reconcile-task-status-on-pr-merge`](reconcile-task-status-on-pr-merge/LESSON.md) — a merged PR does NOT auto-advance its task status; run the closeout the moment you observe the merge — `delegation closeout --decision accept` (delegated) / `advance --status completed` (direct), NEVER hand-edit — and batch-check `gh pr list --state merged` vs task statuses to catch every stale sibling. No PR-merge automation yet (daemon gap tracked in `.agents/proposals/pr-merge-auto-reconcile.md`; observed on swallowed-errors where se9-commands/import + se5-add-errors stayed un-completed after #365/#366/#367 merged).
- [`build-tagged-test-import-cycle`](./build-tagged-test-import-cycle/LESSON.md) — a `//go:build <os>` test file that imports a high-level helper closes an OS-only import cycle invisible to `go vet` on other hosts; catch it locally with `GOOS=windows go vet ./...`, fix by decoupling the low-level package's test from `testutil`, not by removing the new edge.
- [`sonar-container-fsmonitor-socket`](./sonar-container-fsmonitor-socket/LESSON.md) — containerized sonar-scanner intermittently crashes on `.git/fsmonitor--daemon.ipc` (a live Unix socket can't be bind-masked, must be removed); stop the daemon + rm the socket before `docker run`, and watch for orphan-respawn / full Docker-daemon wedge as distinct follow-on failure modes.
- [`stale-plan-status-vs-reality`](./stale-plan-status-vs-reality/LESSON.md) — a plan/spec's `status`/`updated_at` and prose are a claim, not ground truth; before treating a plan as authoritative, diff its stated artifacts/commands against the repo, check TASKS.yaml task-level statuses vs the PLAN.yaml header status, and check `git log --grep=<plan-id>` for post-dated reconciliation the plan file doesn't reflect. Grounded in 4 same-day instances (da-recipe-scripts draft-vs-shipped, endpoint-identity-order-sessions 9/17 completed under `status: draft`, oci-secure-node-provisioning 4/6 completed under `status: draft`, luma-prototype-sprint archived-but-still-cited).
- [`stale-dev-binary-vs-shipped-feature`](./stale-dev-binary-vs-shipped-feature/LESSON.md) — a locally-built dev binary or a plan's stated command name can lag a shipped rename; verify a feature's existence against the shipped binary's `--help`/source or the release CHANGELOG, not just an older build or an unreconciled plan. Grounded in the `da-recipe`→`da run` rename (release 0.5.0, `commands/run.go`) that the `da-recipe-scripts` plan still described under the old working name.
- [`multi-target-family-needs-shared-core`](./multi-target-family-needs-shared-core/LESSON.md) — when a plan produces a FAMILY of near-identical implementations across targets (per-language generators/verifiers, per-platform adapters, per-backend drivers), scope it as a shared core + thin per-target adapters, NOT N independent tasks that each reimplement the shared shape; the r4 eval generator family (`generator-go`/`typescript`/`python` as 3 independent tasks) hit `new_duplicated_lines_density` 18.6%/18.7% (threshold ≤3%) because ~15 scaffolding functions were copied verbatim. Build the core first, then add each thin adapter.
- [`classify-generated-files-before-cleanup`](./classify-generated-files-before-cleanup/LESSON.md) — before `git clean`/`reset --hard` on a "sync clean to master", classify each untracked path (don't assume "not on master → junk"): now-on-master dup → delete; authored artifact in a master-tracked category (lesson/iter-log/fold-back/research) → commit; committed resolved-state contract (`.agentsrc.lock`/`.agentsrc.json`, the `uv.lock` analog) → TRACK (commit), never ignore — `links.EnsureManagedGitignore` `neverIgnored`-filters it back out; live per-machine generated wiring (hook manifests w/ absolute `$HOME` gate.sh paths) → ignore via the da-managed `.gitignore` block (never an ad-hoc root rule), never clean (strips live wiring till next `da refresh`). Grounded in `.github/hooks/*.json` first waved off as "junk" — actually live `da refresh` hook wiring. Snapshot (`git diff --binary`+tar) first.
- [`rendered-rules-edit-the-resource-source`](./rendered-rules-edit-the-resource-source/LESSON.md) — some `~/.agents/rules/<project>/` files are RENDERED (agents.md ← `~/.agents/resources/<project>/AGENTS.md`) and `da refresh` silently reverts direct edits; the repo root AGENTS.md symlinks the rendered layer, hiding the loop. Before editing a rule file, grep `~/.agents/resources/<project>/` for a source copy; edit the source, refresh, verify the render. Grounded in the 2026-07-13 plan-layout fix that vanished on the next refresh. Note this is temporary guidance since it's a product gap / defect.
- [`prescribed-integration-must-obey-dep-direction`](./prescribed-integration-must-obey-dep-direction/LESSON.md) — a delegation bundle that says "call X from Y" is IMPOSSIBLE when Y's package would have to import X's against the dependency DAG (t2c: `internal/platform/codex.go` told to call `commands/import.go` machinery, but `commands`→`platform` is the only cycle-free edge). Author bundles as behavior not call-sites + check import direction; as the worker, don't force it — make the low-level fn self-contained from its own args, cross only in the legal direction (export a predicate, consume from the high layer), duplicate a small format-compatible struct rather than import across the forbidden edge, and flag the re-scope in the merge-back. Sibling of [[build-tagged-test-import-cycle]], [[bundle-scope-via-code-graph]].
- [`worktree-base-predates-task-and-untracked-contract`](./worktree-base-predates-task-and-untracked-contract/LESSON.md) — a worktree worker's checkout only contains what was committed to its base ref at cut time: t2c's worktree had NO `t2c` task entry (base branch predated the task), showed sibling `t2b` as `pending` despite integrated code, and its delegation contract was ABSENT because untracked-in-parent files never git-propagate into a new worktree. Rebase base onto live tip (or spawn off HEAD), deliver the bundle/contract by explicit parent-absolute path not via git, treat in-worktree task status as advisory-only. Inbound dual of [[worktree-isolation-defeats-status-tracking]]; staleness sibling of [[validate-bundle-against-head]].
- [`origin-validate-before-attaching-credentials`](origin-validate-before-attaching-credentials/LESSON.md) — attaching a credential to a request whose URL comes from untrusted input (WWW-Authenticate realm, Location header, redirect target) without origin-validating first leaks it; found 4× in the OCI auth/publish arc (t7/t8). Pin destination scheme+host+effective-port (validate the RESOLVED url — covers scheme-relative `//host`), reject redirects on any credentialed request (Go strips redirect auth by host only — an https→http downgrade or subdomain hop still forwards it), strip query/userinfo from URLs in errors. Every credentialed http.Client needs an explicit CheckRedirect. Invisible to same-model review + green tests; caught only cross-harness ([[cross-harness-read-raw-not-reconciler]]).
- [`no-external-semantics-approximation-for-security-gate`](no-external-semantics-approximation-for-security-gate/LESSON.md) — don't approximate an external tool's semantics (git gitignore matching, a parser, a host matcher) to GATE a fail-closed security decision; every edge-case divergence is a bypass. The t9 CASIgnore fast-path reimplemented gitignore matching to skip the H14 install+verify and cross-harness found a real bypass in 3 straight rounds (symlink / leading-ws / trailing-tab+negation). Prefer: invoke the tool, constrain input to an unambiguous shape (byte-identity/structural invariant), hoist the correct path out of the hot loop, or DROP the optimization. Also: skipping a "redundant" write can drop a load-bearing SIDE EFFECT (the install's canonicalizing rewrite). Caught only cross-harness ([[cross-harness-read-raw-not-reconciler]]); "just perf" next to a fail-closed gate is not exempt.
- [`git-ref-backend-structural-writes-must-mirror`](./git-ref-backend-structural-writes-must-mirror/LESSON.md) — under `work_tracking.backend=git-ref` the ref IS the read source, so EVERY canonical write must mirror to `refs/agents/state`, not just status transitions. The initial cutover mirrored only `runWorkflowAdvance` transitions, so `da workflow task add`/`update` + `plan create` left the ref stale and a subsequent read-modify-write clobbered sequential structural writes (same class as the `read_from: master` footgun). Fix at the canonical-write choke point (`saveCanonicalTasks`/`saveCanonicalPlan`) with overwrite-all + seed-if-absent mirroring keyed by `changedTaskID` (preserves D5 disjoint-task concurrency), mode-aware error (propagate under git-ref, warn under additive write_to=state-ref). Caught only by dogfooding the live cutover (#432 → rollback #433 → fix #434 → reconcile #435). Sibling of [[stale-local-master-ref]].
- [`gated-sonar-patterns-in-test-code`](./gated-sonar-patterns-in-test-code/LESSON.md) — the CI `Enforce zero new Sonar` gate counts godre issues in TEST files too (S3776 cognitive complexity >15, S1192 a ≥10-char literal repeated ≥3×, S8193 a var declared then used only in an `if` condition), and the 95% coverage floor is enforced on NEW non-allowlisted files (brand-new files need ~100%). Inline single-use init-ifs (`if x := f(); x == y`), const-extract repeated literals, and cover defensive error legs via injectable seams (`osReadDir`/`osStat`) so coverage is OS-portable — a Windows-only skip masks rather than fixes. Recurred 4× on the `state_ref_reconcile` test file (#435). Sibling of [[gates-must-be-locally-reproducible]].
- [`read-only-scout-cannot-write-files`](./read-only-scout-cannot-write-files/LESSON.md) — a `scout` (or any agent lacking `write`/`edit`) briefed to persist output via `write`/`local://` silently drops it — the artifact never lands and only a summary survives. Require the deliverable IN THE RETURNED REPORT for read-only agents, or delegate the write to a `task` agent. Match every brief to the agent's actual tool set. Grounded in the 0.5.0 CHANGELOG scouts (2 of 5 lost their per-PR detail to `local://` writes they couldn't perform).
- [`delegated-worker-timeout-salvage-from-worktree`](./delegated-worker-timeout-salvage-from-worktree/LESSON.md) — a long delegated worker `cancelled` at the ~40m runtime cap has usually FINISHED + verified (work sits uncommitted in its worktree), truncated only mid-report. Don't restart: `git status`/`go build`/`go test` in the worktree + read `history://<worker>` tail, then commit+PR it yourself. Scope workers to fit under the cap (split build+full-race-verify+big suite). Happened twice this session (RefBackManagedArtifacts, IterLogRegistry).
- [`cf-worker-jwks-fetch-this-binding`](./cf-worker-auth-gate-fail-closed/LESSON.md) — (added to [[cf-worker-auth-gate-fail-closed]] §6+Meta) a JWKS provider that stores `fetch` and calls `this.fetcher(url)` throws Workers "Illegal invocation" and 403s VALID Access assertions; bind to `globalThis`. Invisible to fixture/StaticJwksProvider + arrow-fetcher unit smokes — only the REAL Cloudflare Access edge (production/preview deploy + real service token) catches it. Rule for edge-auth: local-proof THEN go-live is a mandatory stage. Caught on the obs.agorcha.dev go-live (#469).
- [`register-command-pkg-in-globalflagcov`](./register-command-pkg-in-globalflagcov/LESSON.md) — a new command package wired into the root CLI must be added to the explicit `packages.Load` list in `internal/globalflagcov/static.go` or `TestReportNoUnresolvedHandlers` fails ("unresolved closure"); it's a cross-cutting guard OUTSIDE any command package's write_scope, so a scoped subagent's focused test passes clean and only `go test ./...` at integration catches it. Fixed obs o8 in #467. Sibling of [[lint-check-count-assertion]].
- [`workflow-task-rename-repoint-deps`](./workflow-task-rename-repoint-deps/LESSON.md) — `da workflow task` has no rename; a hand rename that leaves the old id as a pending duplicate AND dependents' `depends_on` pointing at it silently blocks the dependent forever (obs o4 was blocked by a stale o1 dup). On rename: remove the old entry, grep+repoint every dependent's `depends_on` to the new id, re-run `eligible` to confirm the unblock. Sibling of [[consolidate-vestigial-siblings-on-rename]], [[validate-bundle-against-head]].
