---
schema_version: 1
task_id: t2c-codex-import-seam
parent_plan_id: package-artifact-install
title: Route unmarked-collision codex renders into the import-to-scope seam (replace t2b hard-refuse)
summary: 'Replaced t2b''s fail-closed refuse of an unmarked colliding .codex/agents/*.toml with content-aware collision resolution in writeCodexAgentTomlFile (internal/platform/codex.go): a byte-identical unmarked occupant (provably a prior dot-agents render from before codexManagedTomlMarker existed) is silently mark-adopted; a diverged occupant is preserved non-destructively at a sibling .codex-preexisting[-N].toml alternate (RFC section 6 stable naming) plus an import-conflict review note under ~/.agents/review-notes/import-conflicts/, then the managed render lands at the canonical name. commands/import.go''s project-scope import scan (isManagedImportSource) now also skips an already-managed codex toml so da import/da refresh never archives dot-agents'' own renders as foreign content; the preserved alternate sibling is swept up by the SAME existing .codex/agents project-scope import walk on a later pass. resource_plan.go untouched. Full go test ./... green.'
files_changed: []
verification_result:
    status: pass
    summary: 'Structural note for the parent: internal/platform cannot import package commands (commands already imports internal/platform, so the reverse would cycle) — a literal call from codex.go into commands/import.go''s importCandidate/writeImportConflictReviewNote machinery is architecturally impossible. I resolved this by making writeCodexAgentTomlFile fully self-contained (it derives everything it needs from dst alone, since resource_plan.go''s call site — off-limits per the bundle — only ever passes dst/agentMD, no project) and writing a format-identical (same YAML tags) review-note struct locally in codex.go rather than sharing commands.importConflictReviewNote''s type. The review note''s Scope field is the derived repo root path (not a logical project name) since resolving that would require a config.Load() lookup inside a low-level render function with no project parameter threaded through — flagged as a possible follow-up if the parent wants a friendlier Scope value; not required by the verification bullets. I exported platform.IsManagedCodexAgentTomlFile and used it from commands/import.go to skip re-importing dot-agents'' own managed renders — this is the one real cross-package integration point (commands->platform is the only cycle-free direction). refresh.go needed NO changes: runImportFromRefresh already runs unconditionally at the top of runRefresh (line 67), so ''hook via runImportFromRefresh'' was already true structurally before this task. Byte-comparison note requested by the bundle: no marker-stripping/normalization is needed on either side — content (the render body) never carries the marker, and an unmanaged occupant by construction (isManaged==false) also never starts with the marker line, so a raw bytes.Equal(existing, content) is correct; the render body format itself is unchanged by the marker''s addition (marker is a new prepended first line, not a reformat). Also worth flagging: TASKS.yaml in this worktree lacked the t2c task entry entirely (base branch feat/package-artifact-install predates the task''s creation in the parent''s live checkout) and t2b showed status:pending despite its code being verifiably integrated (codexManagedTomlMarker/writeCodexAgentTomlFile present) — I synced both from the parent''s live TASKS.yaml so the workflow CLI could operate; worth reconciling on the parent side. The delegation contract at .agents/active/delegation/t2c-codex-import-seam.yaml was also absent from this worktree (untracked in the parent checkout) — copied over unchanged before verify record.'
integration_notes: 'Structural note for the parent: internal/platform cannot import package commands (commands already imports internal/platform, so the reverse would cycle) — a literal call from codex.go into commands/import.go''s importCandidate/writeImportConflictReviewNote machinery is architecturally impossible. I resolved this by making writeCodexAgentTomlFile fully self-contained (it derives everything it needs from dst alone, since resource_plan.go''s call site — off-limits per the bundle — only ever passes dst/agentMD, no project) and writing a format-identical (same YAML tags) review-note struct locally in codex.go rather than sharing commands.importConflictReviewNote''s type. The review note''s Scope field is the derived repo root path (not a logical project name) since resolving that would require a config.Load() lookup inside a low-level render function with no project parameter threaded through — flagged as a possible follow-up if the parent wants a friendlier Scope value; not required by the verification bullets. I exported platform.IsManagedCodexAgentTomlFile and used it from commands/import.go to skip re-importing dot-agents'' own managed renders — this is the one real cross-package integration point (commands->platform is the only cycle-free direction). refresh.go needed NO changes: runImportFromRefresh already runs unconditionally at the top of runRefresh (line 67), so ''hook via runImportFromRefresh'' was already true structurally before this task. Byte-comparison note requested by the bundle: no marker-stripping/normalization is needed on either side — content (the render body) never carries the marker, and an unmanaged occupant by construction (isManaged==false) also never starts with the marker line, so a raw bytes.Equal(existing, content) is correct; the render body format itself is unchanged by the marker''s addition (marker is a new prepended first line, not a reformat). Also worth flagging: TASKS.yaml in this worktree lacked the t2c task entry entirely (base branch feat/package-artifact-install predates the task''s creation in the parent''s live checkout) and t2b showed status:pending despite its code being verifiably integrated (codexManagedTomlMarker/writeCodexAgentTomlFile present) — I synced both from the parent''s live TASKS.yaml so the workflow CLI could operate; worth reconciling on the parent side. The delegation contract at .agents/active/delegation/t2c-codex-import-seam.yaml was also absent from this worktree (untracked in the parent checkout) — copied over unchanged before verify record.'
created_at: "2026-07-16T02:10:36Z"
---

## Summary

Replaced t2b's fail-closed refuse of an unmarked colliding .codex/agents/*.toml with content-aware collision resolution in writeCodexAgentTomlFile (internal/platform/codex.go): a byte-identical unmarked occupant (provably a prior dot-agents render from before codexManagedTomlMarker existed) is silently mark-adopted; a diverged occupant is preserved non-destructively at a sibling .codex-preexisting[-N].toml alternate (RFC section 6 stable naming) plus an import-conflict review note under ~/.agents/review-notes/import-conflicts/, then the managed render lands at the canonical name. commands/import.go's project-scope import scan (isManagedImportSource) now also skips an already-managed codex toml so da import/da refresh never archives dot-agents' own renders as foreign content; the preserved alternate sibling is swept up by the SAME existing .codex/agents project-scope import walk on a later pass. resource_plan.go untouched. Full go test ./... green.

## Integration Notes

Structural note for the parent: internal/platform cannot import package commands (commands already imports internal/platform, so the reverse would cycle) — a literal call from codex.go into commands/import.go's importCandidate/writeImportConflictReviewNote machinery is architecturally impossible. I resolved this by making writeCodexAgentTomlFile fully self-contained (it derives everything it needs from dst alone, since resource_plan.go's call site — off-limits per the bundle — only ever passes dst/agentMD, no project) and writing a format-identical (same YAML tags) review-note struct locally in codex.go rather than sharing commands.importConflictReviewNote's type. The review note's Scope field is the derived repo root path (not a logical project name) since resolving that would require a config.Load() lookup inside a low-level render function with no project parameter threaded through — flagged as a possible follow-up if the parent wants a friendlier Scope value; not required by the verification bullets. I exported platform.IsManagedCodexAgentTomlFile and used it from commands/import.go to skip re-importing dot-agents' own managed renders — this is the one real cross-package integration point (commands->platform is the only cycle-free direction). refresh.go needed NO changes: runImportFromRefresh already runs unconditionally at the top of runRefresh (line 67), so 'hook via runImportFromRefresh' was already true structurally before this task. Byte-comparison note requested by the bundle: no marker-stripping/normalization is needed on either side — content (the render body) never carries the marker, and an unmanaged occupant by construction (isManaged==false) also never starts with the marker line, so a raw bytes.Equal(existing, content) is correct; the render body format itself is unchanged by the marker's addition (marker is a new prepended first line, not a reformat). Also worth flagging: TASKS.yaml in this worktree lacked the t2c task entry entirely (base branch feat/package-artifact-install predates the task's creation in the parent's live checkout) and t2b showed status:pending despite its code being verifiably integrated (codexManagedTomlMarker/writeCodexAgentTomlFile present) — I synced both from the parent's live TASKS.yaml so the workflow CLI could operate; worth reconciling on the parent side. The delegation contract at .agents/active/delegation/t2c-codex-import-seam.yaml was also absent from this worktree (untracked in the parent checkout) — copied over unchanged before verify record.
