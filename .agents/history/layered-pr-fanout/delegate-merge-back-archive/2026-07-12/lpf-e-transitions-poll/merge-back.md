---
schema_version: 1
task_id: lpf-e-transitions-poll
parent_plan_id: layered-pr-fanout
title: Verifier/lens/owner transition split + PR poll detector (§6, §3.2)
summary: 'Implemented §6 verifier/lens/owner transition-owner split + §3.2 PR poll detector in commands/workflow/transitions.go. verifierTransition owns in_progress->awaiting_agent_review (gated on §2.3: PR open + rollup GREEN + Sonar OK + 0 OPEN issues); lensGateTransition owns awaiting_agent_review->awaiting_owner_review on all-accept or ->in_progress on any reject; runPollDetector/pollEventTransition consume event.pr.* envelopes with NO gh calls -> merged->completed, review_requested_change->in_progress, closed->blocked+cascade (§2.6), force_rebased->in_progress+rebase-only (§7.3); detectForceRebase does the §2.7 SHA1 set-diff. Each owner refuses sources it does not own (DC#8) and re-validates every edge against the §3.1 machine in task_status.go. Table-driven tests for each owner + poll detector. go test ./commands/workflow/... green.'
files_changed: []
verification_result:
    status: pass
    summary: 'Worktree was 111 commits behind master and missing completed deps (internal/events/pr.go from lpf-pr-producer; commands/workflow/base_resolution.go from lpf-d); reset worktree HARD to master before implementing so transitions.go compiles against the real event.pr.* surface. Commit f34bd5e1 contains ONLY commands/workflow/transitions.go + transitions_test.go (the write_scope). The §3.4.5 auto-resume predicate evaluators the task note mentions are ALREADY implemented in blocked_on.go (lpf-c, completed) — re-implementing them would be out-of-scope duplication, so left untouched. transitionDecision is a pure value (no persistence/IO): the parent wires decisions into TASKS.yaml mutation + downstream cascade/rebase enqueue at the call site. NO push performed; commit is local to the worktree branch per the serialized-push rule.'
integration_notes: 'Worktree was 111 commits behind master and missing completed deps (internal/events/pr.go from lpf-pr-producer; commands/workflow/base_resolution.go from lpf-d); reset worktree HARD to master before implementing so transitions.go compiles against the real event.pr.* surface. Commit f34bd5e1 contains ONLY commands/workflow/transitions.go + transitions_test.go (the write_scope). The §3.4.5 auto-resume predicate evaluators the task note mentions are ALREADY implemented in blocked_on.go (lpf-c, completed) — re-implementing them would be out-of-scope duplication, so left untouched. transitionDecision is a pure value (no persistence/IO): the parent wires decisions into TASKS.yaml mutation + downstream cascade/rebase enqueue at the call site. NO push performed; commit is local to the worktree branch per the serialized-push rule.'
created_at: "2026-06-22T13:18:55Z"
---

## Summary

Implemented §6 verifier/lens/owner transition-owner split + §3.2 PR poll detector in commands/workflow/transitions.go. verifierTransition owns in_progress->awaiting_agent_review (gated on §2.3: PR open + rollup GREEN + Sonar OK + 0 OPEN issues); lensGateTransition owns awaiting_agent_review->awaiting_owner_review on all-accept or ->in_progress on any reject; runPollDetector/pollEventTransition consume event.pr.* envelopes with NO gh calls -> merged->completed, review_requested_change->in_progress, closed->blocked+cascade (§2.6), force_rebased->in_progress+rebase-only (§7.3); detectForceRebase does the §2.7 SHA1 set-diff. Each owner refuses sources it does not own (DC#8) and re-validates every edge against the §3.1 machine in task_status.go. Table-driven tests for each owner + poll detector. go test ./commands/workflow/... green.

## Integration Notes

Worktree was 111 commits behind master and missing completed deps (internal/events/pr.go from lpf-pr-producer; commands/workflow/base_resolution.go from lpf-d); reset worktree HARD to master before implementing so transitions.go compiles against the real event.pr.* surface. Commit f34bd5e1 contains ONLY commands/workflow/transitions.go + transitions_test.go (the write_scope). The §3.4.5 auto-resume predicate evaluators the task note mentions are ALREADY implemented in blocked_on.go (lpf-c, completed) — re-implementing them would be out-of-scope duplication, so left untouched. transitionDecision is a pure value (no persistence/IO): the parent wires decisions into TASKS.yaml mutation + downstream cascade/rebase enqueue at the call site. NO push performed; commit is local to the worktree branch per the serialized-push rule.
