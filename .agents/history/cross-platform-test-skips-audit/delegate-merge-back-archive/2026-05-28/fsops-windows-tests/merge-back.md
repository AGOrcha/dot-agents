---
schema_version: 1
task_id: fsops-windows-tests
parent_plan_id: cross-platform-test-skips-audit
title: Add internal/fsops/fsops_windows_test.go (6 untested Windows-build-tagged functions)
summary: 'Added internal/fsops/fsops_windows_test.go (//go:build windows, 314 lines, 11 tests) covering systemExe, MkdirAll, mkdirAllComponents, WriteFile, Remove, RemoveAll. Mirrors the POSIX round-trip test plus mkdirAllComponents deep-path/idempotent/file-as-parent cases, systemExe %SystemRoot% set+unset branches and winPowerShell anchoring regression, IsNotExist tolerance for Remove/RemoveAll, the missing-parent → MkdirAll → fallback chain in WriteFile, and 4 deny-ACE negative paths via testutil.MakeDirUnreadable. Verified on macOS: go build ./... clean; GOOS=windows go vet ./internal/fsops/ clean; GOOS=windows go test -c ./internal/fsops/ cross-compiles to windows/amd64; gofmt -l clean; POSIX go test -race -count=1 ./internal/fsops/ passes (1.349s). Real execution happens on windows-latest CI. PR #73 open: https://github.com/NikashPrakash/dot-agents/pull/73. Branch feature/fsops-windows-tests, commit 8d1a3faa. Anti-scope honoured: no *.go production file under internal/fsops/ touched. Unblocks cg6-prune for the internal/fsops/fsops_windows.go allowlist entry once Windows CI confirms coverage > 0.'
files_changed: []
verification_result:
    status: pass
    summary: 'Do NOT merge until windows-latest CI passes — these tests are skipped at compile time on POSIX hosts so green local CI does not prove the test bodies are correct. Parent should: (1) wait for windows-latest workflow to run on PR #73, (2) confirm internal/fsops/fsops_windows.go coverage > 0% in the report, (3) confirm none of the deny-ACE tests trigger the t.Skip path on the runner (which would mean the fixture silently failed). The deny-ACE branches in Remove/RemoveAll/WriteFile intentionally tolerate PowerShell-fallback success with t.Skip — if every runner skips, the parent should consider tightening the assertion. Once merged, cg6-prune can drop the internal/fsops/fsops_windows.go [platform-tag] 0.00% allowlist entry.'
integration_notes: 'Do NOT merge until windows-latest CI passes — these tests are skipped at compile time on POSIX hosts so green local CI does not prove the test bodies are correct. Parent should: (1) wait for windows-latest workflow to run on PR #73, (2) confirm internal/fsops/fsops_windows.go coverage > 0% in the report, (3) confirm none of the deny-ACE tests trigger the t.Skip path on the runner (which would mean the fixture silently failed). The deny-ACE branches in Remove/RemoveAll/WriteFile intentionally tolerate PowerShell-fallback success with t.Skip — if every runner skips, the parent should consider tightening the assertion. Once merged, cg6-prune can drop the internal/fsops/fsops_windows.go [platform-tag] 0.00% allowlist entry.'
created_at: "2026-05-25T13:59:19Z"
---

## Summary

Added internal/fsops/fsops_windows_test.go (//go:build windows, 314 lines, 11 tests) covering systemExe, MkdirAll, mkdirAllComponents, WriteFile, Remove, RemoveAll. Mirrors the POSIX round-trip test plus mkdirAllComponents deep-path/idempotent/file-as-parent cases, systemExe %SystemRoot% set+unset branches and winPowerShell anchoring regression, IsNotExist tolerance for Remove/RemoveAll, the missing-parent → MkdirAll → fallback chain in WriteFile, and 4 deny-ACE negative paths via testutil.MakeDirUnreadable. Verified on macOS: go build ./... clean; GOOS=windows go vet ./internal/fsops/ clean; GOOS=windows go test -c ./internal/fsops/ cross-compiles to windows/amd64; gofmt -l clean; POSIX go test -race -count=1 ./internal/fsops/ passes (1.349s). Real execution happens on windows-latest CI. PR #73 open: https://github.com/NikashPrakash/dot-agents/pull/73. Branch feature/fsops-windows-tests, commit 8d1a3faa. Anti-scope honoured: no *.go production file under internal/fsops/ touched. Unblocks cg6-prune for the internal/fsops/fsops_windows.go allowlist entry once Windows CI confirms coverage > 0.

## Integration Notes

Do NOT merge until windows-latest CI passes — these tests are skipped at compile time on POSIX hosts so green local CI does not prove the test bodies are correct. Parent should: (1) wait for windows-latest workflow to run on PR #73, (2) confirm internal/fsops/fsops_windows.go coverage > 0% in the report, (3) confirm none of the deny-ACE tests trigger the t.Skip path on the runner (which would mean the fixture silently failed). The deny-ACE branches in Remove/RemoveAll/WriteFile intentionally tolerate PowerShell-fallback success with t.Skip — if every runner skips, the parent should consider tightening the assertion. Once merged, cg6-prune can drop the internal/fsops/fsops_windows.go [platform-tag] 0.00% allowlist entry.
