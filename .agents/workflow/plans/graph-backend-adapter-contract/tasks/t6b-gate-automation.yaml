schema_version: 1
plan_id: graph-backend-adapter-contract
order: 11
task:
    id: t6b-gate-automation
    title: Promote CRG parity matrix and behavior harness into CI
    status: completed
    depends_on:
        - t6a-parity-surfaces
    blocks:
        - t6d-final-bridge-deletion
    owner: dot-agents
    write_scope:
        - .github/workflows/test.yml
        - testdata/crg-parity/
    verification_required: true
    notes: 'Wire corrected parity matrix + behavior-preservation harness into CI so the decommission soak clock can start. Starts the evidence clock; does NOT claim the multi-week gate satisfied. Anti-scope: no bridge deletion; do not mark soak complete in the same change that introduces the gate.'
    app_type: go-cli
