schema_version: 1
plan_id: orchestration-companion-stop-hooks
order: 8
task:
    id: release-minor
    title: 'Release: bump VERSION + finalize CHANGELOG for the companion-hooks minor'
    status: pending
    depends_on:
        - t0-upstream-readback-and-starter-assets
        - t1-companion-sentinel-operations
        - t2-orchestrator-handoff-gate
        - t3-delegation-closeout-gate
        - t4-wave-picker-no-hook-assessment
        - t5-integration-and-outcome-compatibility
        - t-cross-plan-readback
        - t-operator-docs
    blocks: []
    owner: dot-agents
    write_scope:
        - VERSION
        - CHANGELOG.md
    verification_required: true
    notes: |-
        Release-gated tail task (hybrid convention, MINOR path). This plan
        delivers new user-visible surface — two companion terminal gates
        (orchestrator handoff, delegation closeout) plus orchestrator-session-start
        and plan-wave-picker shipped in the starter, and bounded pre-action
        guards. New gate + new starter skill contracts are additive surface,
        MINOR not MAJOR (no public surface removed or incompatibly changed).
        Target version = next minor computed from current VERSION at
        ready-time (do NOT hardcode). With VERSION at 0.3.2 today the next
        minor is 0.4.0, but resolve from VERSION when ready; serialize at the
        VERSION bump if another minor ships first.
        Action: bump VERSION to the resolved next minor and finalize the
        CHANGELOG "## [<target>]" section. Merge to master fires
        auto-release.yml. Note: depends on loop-discipline-stop-hooks
        scaffold assets shipping first.
    app_type: release
