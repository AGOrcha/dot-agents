schema_version: 1
plan_id: cross-platform-test-skips-audit
order: 7
task:
    id: cg6-prune
    title: Prune stale allowlist entries the migration unblocks
    status: completed
    depends_on:
        - migrate-sites
    blocks: []
    owner: dot-agents
    write_scope:
        - scripts/coverage-exceptions.txt
    verification_required: true
    notes: |-
        After migrate-sites lifts Windows coverage on the affected files, the coverage-gate prints STALE-ALLOWLIST for any entry now at or above threshold. Prune those entries — that is the cg6 ratchet tightening. Cross-reference with the R1 carry-over entries this PR added (internal/scoring/persist.go [defensive-guards], internal/scoring/objective_checks.go [legacy-tail], commands/agents/cmd.go, commands/workflow/prefs.go).

        No stale allowlist entries at HEAD (master 6859c01). Reproduced CI merged multi-OS coverage profile; all 23 allowlisted files still sub-95% — nothing to prune. Driving the *_windows.go files to 95% is a separate ratchet task, not an allowlist prune.
    app_type: go-cli
