schema_version: 1
id: extends-oci-relax
title: Relax extends to accept OCI (full source-kind orthogonality)
status: complete
summary: Allow extends (config layers) to be sourced from OCI too, so an org can keep its entire agent config (layers + artifacts) in one OCI registry as the SOT; removes the last source/kind asymmetry
created_at: "2026-06-23T22:51:49Z"
updated_at: "2026-07-04T16:22:52Z"
owner: dot-agents
success_criteria: extends accepts an oci source; a config layer can be pulled from OCI and merged like any layer; no source/kind asymmetry remains; spec §15 amended (D13), docs+comments updated; existing extends-from-git/http/local unaffected
verification_strategy: 'Go tests: SelectFetcher accepts oci + an oci-layer fetch returns mergeable content; resolver merges an oci-sourced layer; the old rejection test flips; docs/spec build'
current_focus_task: ""
