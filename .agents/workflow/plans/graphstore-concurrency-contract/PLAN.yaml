schema_version: 1
id: graphstore-concurrency-contract
title: Graphstore Store contract + Path-A ephemeral provider (enforced bounds + timeout)
status: active
summary: 'Decision C (hybrid): publish a stable backend/strategy-agnostic Store contract with hard bounds + request timeout; implement Path A (ephemeral, lazy/cheap) behind it; bind callers + Deps; defer the Path B daemon as a transparent provider swap.'
created_at: "2026-06-08T03:22:02Z"
updated_at: "2026-06-08T03:22:35Z"
owner: dot-agents
success_criteria: One published Store contract all callers + Deps bind to; bounds hard + uniform across native/CRG + request timeout honored (regression tests prove cap + cross-path parity); concurrency model documented; maxNodes Low-1 + di-refactor OD-1 closed via the contract.
verification_strategy: go test ./internal/graphstore/... + caller packages; cross-path parity + hard-cap + timeout regression tests; go vet; per-package 95% gate.
current_focus_task: ""
