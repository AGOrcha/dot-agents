schema_version: 1
plan_id: agent-ops-hardening
order: 4
task:
    id: p1-delegation-brief-hardening-skill
    title: 'P1: operationalize pre-fanout bundle hardening (make validate-bundle-against-head + bundle-scope-via-code-graph mandatory in orchestrator-session-start/delegation-lifecycle)'
    status: awaiting_owner_review
    depends_on: []
    blocks: []
    owner: dot-agents
    write_scope:
        - internal/scaffold/home/starter/skills/global/orchestrator-session-start/
        - internal/scaffold/home/starter/skills/global/delegation-lifecycle/
    verification_required: true
    notes: |-
        skill source lives in internal/scaffold/...; it materializes to ~/.agents via `da refresh`. Editing `.agents/skills/` directly is not committable (gitignored).

        Extend existing skills, do NOT create a new sibling skill. The behavior already exists as instruction units
        validate-bundle-against-head + bundle-scope-via-code-graph in orchestrator-session-start; the gap is making them
        MANDATORY pre-fanout (in orchestrator-session-start/delegation-lifecycle), adding a coverage-delta-forecast
        bullet (walk all *_test.go callers of changed/deleted symbols -> the co-commit wall; refuse a bundle whose
        asserting tests fall outside scope), and a brief-template line. Retires the largest fold-back cluster.

        Impl done; delivered with PR #134 (orchestrator agent-type + consolidated pre-fanout gate), open, awaiting owner merge.
    app_type: skill
