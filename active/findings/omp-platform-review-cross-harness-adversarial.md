# omp-platform findings — review (cross-harness adversarial) stage

- `./bin/da --json workflow resolve-prompt --kind reviewer --slug cross-harness-adversarial` resolved only the repo-local overlay (`cross-harness-adversarial.project.md`); both the shared reviewer base and the shared cross-harness lens entries came back `scope:"unresolved", exists:false`. The orchestration still expects a different-model pass, but da gives no explicit degraded-state signal beyond those empty entries. Review ergonomics would be better if `resolve-prompt` surfaced "overlay-only" as a first-class warning.
- Cross-model review under omp is workable, but evidence gathering is still manual: I had to correlate `SharedTargetIntents`, `CountLinks`, and `ManagedOutputs` by source reads/grep to prove the Copilot `.agents/skills/` omission. A structural ownership report (e.g. "for platform X, show repo-local outputs from CreateLinks + SharedTargetIntents + CountLinks") would make diff-model reviewers much faster and less error-prone.
