This parent directory is deliberately named historical.yaml. mergeHistorical's
os.ReadFile fails with a non-NotExist error because the path is a directory,
exercising the historical-archive read-error path.
